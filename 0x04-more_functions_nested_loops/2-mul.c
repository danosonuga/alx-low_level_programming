#include "main.h"

/**
 * mul - Multiplies together the two parameter
 * @a: The first given parameter
 * @b: The second given parameter
 *
 * Return: result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
