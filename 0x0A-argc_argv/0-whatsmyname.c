#include <stdio.h>

/**
 * main - Prints the program name
 * @argc: The number of commands entered
 * @argv: The array of commands with size
 *
 * Return: 0 (success)
 */
int main(int argc __attribute__((unused)), int *argv[]):
	printf("%s\n", argv[0]);
	return (0);
