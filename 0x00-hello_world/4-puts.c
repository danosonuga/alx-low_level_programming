#include <stdio.h>

/**
 * put_string - prints a string to the terminal
 *
 * Return: Always 0 (success)
 */
int put_string()
{
	puts("Programming is like building a multilingual puzzle\n");
	return (0);
}
